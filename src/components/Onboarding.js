import React from 'react';
import { View } from 'react-native';
import Title from './Title';

// define onboarding page calling title component here
const Onboarding = () => {
    return (
        <View>
            <Title text="Listr" />
        </View>
    );
}

export default Onboarding